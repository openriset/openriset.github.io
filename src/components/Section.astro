---
interface Props {
  id: string;
  title: string;
  subtitle?: string;
}

const { id, title, subtitle } = Astro.props;
---

<section {id} class="py-12 sm:py-16 md:py-20 lg:py-24">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="max-w-3xl mx-auto text-center mb-10 sm:mb-14 lg:mb-16">
      <h2 class="text-2xl sm:text-3xl lg:text-4xl font-bold text-or-slate-900 leading-tight tracking-tight">
        {title}
      </h2>
      {subtitle && (
        <p class="mt-3 sm:mt-4 text-sm sm:text-base lg:text-lg text-or-slate-500 leading-relaxed max-w-2xl mx-auto">
          {subtitle}
        </p>
      )}
    </div>
    <slot />
  </div>
</section>
